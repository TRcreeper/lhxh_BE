<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lhxh.demo.mapper.ApplyMapper">
    <!-- 动态sql -->
    <select id="list" resultType="com.lhxh.demo.pojo.Apply">
        select * from apply
        <!-- <where>
                <if test="nickname!=null">
                        nickname LIKE CONCAT('%', #{nickname}, '%')
                </if>
        </where> -->
     </select>   
</mapper>